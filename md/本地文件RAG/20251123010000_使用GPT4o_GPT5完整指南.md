# 🤖 使用 GPT-4o、GPT-4 Turbo 或 GPT-5 完整指南

## 🎯 概述

本指南将帮助你在 LocalFileRAG 知识库问答系统中使用 OpenAI 的模型，包括：
- ✅ GPT-4o (最新多模态模型)
- ✅ GPT-4 Turbo (高性能版本)
- ✅ GPT-4 (标准版)
- ✅ GPT-3.5 Turbo (经济版)
- ✅ 未来的 GPT-5 (发布后自动支持)

---

## 🚀 快速开始（3步完成）

### 步骤 1: 获取 OpenAI API Key

#### 方式1: 官方渠道（推荐）

1. 访问 OpenAI 官网: https://platform.openai.com
2. 注册/登录账号
3. 进入 API Keys 页面: https://platform.openai.com/api-keys
4. 点击 "Create new secret key"
5. 复制生成的 API Key (格式: `sk-xxx...`)

⚠️ **重要**: API Key 只显示一次，请妥善保存

#### 方式2: 使用代理服务

如果无法访问 OpenAI 官方，可以使用第三方代理服务：
- API2D (https://api2d.com)
- OpenAI-SB (https://openai-sb.com)
- 其他符合 OpenAI API 格式的服务

### 步骤 2: 设置 API Key

#### Windows

**方式A: 临时设置（当前会话）**
```powershell
# PowerShell
$env:OPENAI_API_KEY="sk-your-openai-api-key-here"

# CMD
set OPENAI_API_KEY=sk-your-openai-api-key-here
```

**方式B: 永久设置（推荐）**
```powershell
# 打开环境变量设置
1. Win + R 输入 "sysdm.cpl"
2. 点击 "高级" -> "环境变量"
3. 在 "用户变量" 中点击 "新建"
4. 变量名: OPENAI_API_KEY
5. 变量值: sk-your-openai-api-key-here
6. 点击 "确定"

# 重启终端使其生效
```

#### Linux/macOS

**方式A: 临时设置**
```bash
export OPENAI_API_KEY="sk-your-openai-api-key-here"
```

**方式B: 永久设置（推荐）**
```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
echo 'export OPENAI_API_KEY="sk-your-openai-api-key-here"' >> ~/.bashrc

# 使其生效
source ~/.bashrc

# 验证设置
echo $OPENAI_API_KEY
```

### 步骤 3: 配置应用

编辑 `application.yml`:

```yaml
knowledge:
  qa:
    llm:
      # 设置 provider 为 openai
      provider: openai
      
      # API Key 从环境变量读取
      api-key: ${OPENAI_API_KEY:}
      
      # API 端点（默认即可）
      api-url: https://api.openai.com/v1/chat/completions
      
      # 选择模型
      model: gpt-4o  # 或 gpt-4-turbo, gpt-4, gpt-3.5-turbo
```

---

## ▶️ 运行应用

### 1. 启动应用

```bash
cd D:\Jetbrains\hackathon\AI-Reviewer\ai-reviewer-base-file-rag

# 使用 Maven 启动
mvn spring-boot:run
```

### 2. 查看启动日志

**成功启动日志**:
```
🤖 创建 OpenAI LLM 客户端
   - 模型: gpt-4o
✅ OpenAI LLM 客户端初始化完成
```

**如果出现问题**:
```
⚠️  未配置 OpenAI API Key
💡 提示: 设置环境变量 OPENAI_API_KEY 或 AI_API_KEY
💡 将使用 Mock 客户端
```
→ 检查环境变量是否正确设置

---

## 🎯 支持的模型详解

### GPT-4o (推荐) ⭐⭐⭐⭐⭐

**特点**:
- 最新多模态模型（2024年5月发布）
- 支持文本、图像、音频输入
- 性能优异，速度快
- 上下文窗口: 128K tokens
- 价格适中

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4o
```

**适用场景**:
- ✅ 生产环境
- ✅ 需要高质量回答
- ✅ 预算充足

**成本** (2024年11月):
- 输入: $2.50 / 1M tokens
- 输出: $10.00 / 1M tokens

### GPT-4o-mini (经济版) ⭐⭐⭐⭐

**特点**:
- GPT-4o 的轻量版
- 速度更快，成本更低
- 上下文窗口: 128K tokens
- 性能接近 GPT-4

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4o-mini
```

**适用场景**:
- ✅ 开发测试
- ✅ 大量查询
- ✅ 成本敏感

**成本**:
- 输入: $0.15 / 1M tokens
- 输出: $0.60 / 1M tokens

### GPT-4 Turbo ⭐⭐⭐⭐⭐

**特点**:
- GPT-4 的高性能版本
- 上下文窗口: 128K tokens
- 知识更新到 2024年4月
- 推理能力强

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4-turbo
```

**适用场景**:
- ✅ 复杂推理任务
- ✅ 长文本处理
- ✅ 高质量要求

**成本**:
- 输入: $10.00 / 1M tokens
- 输出: $30.00 / 1M tokens

### GPT-4 (标准版) ⭐⭐⭐⭐

**特点**:
- 经典 GPT-4 模型
- 上下文窗口: 8K tokens
- 可靠稳定

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4
```

### GPT-3.5 Turbo (最经济) ⭐⭐⭐

**特点**:
- 最经济的选择
- 速度快
- 上下文窗口: 16K tokens

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-3.5-turbo
```

**适用场景**:
- ✅ 开发测试
- ✅ 预算有限
- ✅ 简单问答

**成本**:
- 输入: $0.50 / 1M tokens
- 输出: $1.50 / 1M tokens

### GPT-5 (未来) 🚀

**配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-5  # 发布后直接可用
```

---

## 🔧 高级配置

### 1. 使用自定义 API 端点

如果使用代理服务或私有部署：

```yaml
knowledge:
  qa:
    llm:
      provider: openai
      api-url: https://your-proxy.com/v1/chat/completions
      model: gpt-4o
```

**示例（API2D）**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      api-url: https://oa.api2d.net/v1/chat/completions
      api-key: ${API2D_KEY:}
      model: gpt-4o
```

### 2. 调整上下文长度

```yaml
knowledge:
  qa:
    llm:
      model: gpt-4o
      # 调整上下文长度（根据模型限制）
      max-context-length: 50000  # GPT-4o 支持 128K
      max-doc-length: 10000
```

### 3. 多环境配置

**开发环境** (使用 GPT-3.5):
```yaml
# application-dev.yml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-3.5-turbo  # 开发用经济模型
```

**生产环境** (使用 GPT-4o):
```yaml
# application-prod.yml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4o  # 生产用高质量模型
```

**启动**:
```bash
# 开发环境
mvn spring-boot:run -Dspring.profiles.active=dev

# 生产环境
mvn spring-boot:run -Dspring.profiles.active=prod
```

---

## 🧪 测试 API

### 方式1: 使用 REST API

```bash
# 1. 启动应用
mvn spring-boot:run

# 2. 索引文档
curl -X POST http://localhost:8080/api/files/index \
  -F "file=@document.pdf"

# 3. 提问（使用 GPT-4o）
curl -X POST http://localhost:8080/api/qa/ask \
  -H "Content-Type: application/json" \
  -d '{
    "question": "文档的主要内容是什么？"
  }'
```

### 方式2: 使用命令行测试

```bash
# 验证 API Key
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# 测试聊天
curl https://api.openai.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

---

## 💰 成本计算

### 估算公式

```
每次问答成本 = (输入 tokens × 输入价格 + 输出 tokens × 输出价格) / 1,000,000
```

### 实际示例

**场景**: 基于 10 份文档（每份 5000 字符 ≈ 1250 tokens）回答问题

| 模型 | 输入 tokens | 输出 tokens | 单次成本 | 1000次成本 |
|------|------------|------------|---------|-----------|
| **gpt-4o** | 12,500 | 500 | $0.036 | $36 |
| **gpt-4o-mini** | 12,500 | 500 | $0.002 | $2 |
| **gpt-4-turbo** | 12,500 | 500 | $0.140 | $140 |
| **gpt-3.5-turbo** | 12,500 | 500 | $0.007 | $7 |

### 成本优化建议

1. **开发测试**: 使用 `gpt-3.5-turbo` 或 `gpt-4o-mini`
2. **生产环境**: 使用 `gpt-4o` (性价比最高)
3. **关键任务**: 使用 `gpt-4-turbo` (最高质量)
4. **限制文档数量**: 只检索最相关的文档
5. **缓存结果**: 相同问题返回缓存答案

---

## ⚠️ 常见问题

### 问题1: API Key 无效

**错误信息**:
```
OpenAI API 错误: HTTP 401, Invalid API key
```

**解决方案**:
1. 检查 API Key 是否正确
2. 确认环境变量已设置: `echo $OPENAI_API_KEY`
3. 重启应用

### 问题2: 请求超时

**错误信息**:
```
java.net.SocketTimeoutException: timeout
```

**解决方案**:
```java
// 在 OpenAILLMClient 中增加超时时间
private static final int DEFAULT_TIMEOUT = 120; // 改为 120 秒
```

### 问题3: 模型不存在

**错误信息**:
```
OpenAI API 错误: model not found
```

**解决方案**:
1. 检查模型名称是否正确
2. 确认账号有权限使用该模型
3. 参考官方文档: https://platform.openai.com/docs/models

### 问题4: 额度不足

**错误信息**:
```
You exceeded your current quota
```

**解决方案**:
1. 检查账号余额: https://platform.openai.com/account/billing
2. 充值或升级账号
3. 临时切换到 `gpt-3.5-turbo`

### 问题5: 网络访问问题

**错误信息**:
```
java.net.UnknownHostException: api.openai.com
```

**解决方案**:
1. 检查网络连接
2. 使用代理服务
3. 配置系统代理

---

## 🎯 最佳实践

### 1. 选择合适的模型

```
简单问答 → gpt-3.5-turbo
常规问答 → gpt-4o (推荐)
复杂推理 → gpt-4-turbo
开发测试 → gpt-4o-mini
```

### 2. 优化 Prompt

```java
// 在 KnowledgeQAService 中优化系统提示
String systemPrompt = """
    你是一个专业的知识库助手。
    请基于提供的文档内容回答问题，确保：
    1. 答案准确，有据可依
    2. 简洁明了，重点突出
    3. 如果文档中没有相关信息，请明确说明
    """;
```

### 3. 控制上下文大小

```yaml
knowledge:
  qa:
    llm:
      # 限制上下文，降低成本
      max-context-length: 15000  # 而不是 50000
      max-doc-length: 3000       # 而不是 10000
    
    vector-search:
      # 只检索最相关的文档
      top-k: 10  # 而不是 20
```

### 4. 监控使用量

```java
// 添加使用量统计
@Component
public class OpenAIUsageMonitor {
    private AtomicLong totalRequests = new AtomicLong(0);
    private AtomicLong totalTokens = new AtomicLong(0);
    
    public void recordUsage(long tokens) {
        totalRequests.incrementAndGet();
        totalTokens.addAndGet(tokens);
    }
}
```

---

## 📊 性能对比

| 模型 | 速度 | 质量 | 成本 | 推荐度 |
|------|------|------|------|--------|
| **gpt-4o** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **gpt-4o-mini** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **gpt-4-turbo** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **gpt-4** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| **gpt-3.5-turbo** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## ✅ 检查清单

使用此清单确保 OpenAI 正确配置:

- [ ] ✅ 已获取 OpenAI API Key
- [ ] ✅ 环境变量已设置
- [ ] ✅ application.yml 已配置 provider=openai
- [ ] ✅ 已选择合适的模型
- [ ] ✅ 能看到 "OpenAI LLM 客户端初始化完成" 日志
- [ ] ✅ 测试问答成功
- [ ] ✅ 账号余额充足

---

## 📚 相关资源

### 官方文档
- OpenAI Platform: https://platform.openai.com
- API 文档: https://platform.openai.com/docs/api-reference
- 模型列表: https://platform.openai.com/docs/models
- 价格: https://openai.com/pricing

### 社区资源
- OpenAI Discord: https://discord.com/invite/openai
- OpenAI Forum: https://community.openai.com

---

## 🎉 总结

通过以上步骤，你已经可以:

1. ✅ 使用 GPT-4o 进行智能问答
2. ✅ 根据需求选择合适的模型
3. ✅ 优化成本和性能
4. ✅ 解决常见问题

**推荐配置**:
```yaml
knowledge:
  qa:
    llm:
      provider: openai
      model: gpt-4o  # 性价比最高
      api-key: ${OPENAI_API_KEY:}
```

**下一步**:
- 尝试不同的模型
- 优化 Prompt 提示词
- 监控使用量和成本
- 准备迁移到 GPT-5 (发布后)

---

**文档版本**: v1.0  
**更新时间**: 2025-11-23  
**适用版本**: LocalFileRAG v1.0+

