# 📘 Tesseract OCR 使用完整指南

## 🎯 概述

本指南将帮助你在 LocalFileRAG 框架中启用 Tesseract OCR 进行图片文字识别。

---

## 📋 前提条件

- ✅ 已有 LocalFileRAG 项目
- ✅ Java 8+ 环境
- ✅ Maven 构建工具

---

## 🚀 快速开始（3步完成）

### 步骤 1: 安装 Tesseract OCR

#### Windows

##### 方式1: 使用安装包（推荐）

1. 下载 Tesseract 安装包:
   - 官方: https://github.com/UB-Mannheim/tesseract/wiki
   - 推荐版本: `tesseract-ocr-w64-setup-5.3.3.20231005.exe`

2. 运行安装程序
   - 默认安装路径: `C:\Program Files\Tesseract-OCR`
   - ⚠️ **重要**: 安装时勾选 "Additional language data"
   - 勾选 `Chinese - Simplified` (简体中文)
   - 勾选 `English` (英文)

3. 添加到系统环境变量 (可选但推荐):
   ```cmd
   # 添加到 PATH
   set PATH=%PATH%;C:\Program Files\Tesseract-OCR
   ```

4. 验证安装:
   ```cmd
   tesseract --version
   ```
   
   **输出示例**:
   ```
   tesseract 5.3.3
   leptonica-1.82.0
   ```

##### 方式2: 使用 Chocolatey

```powershell
# 安装 Tesseract
choco install tesseract

# 安装中文语言包
choco install tesseract-ocr-chi-sim
```

#### Linux (Ubuntu/Debian)

```bash
# 更新包列表
sudo apt-get update

# 安装 Tesseract
sudo apt-get install -y tesseract-ocr

# 安装中文简体语言包
sudo apt-get install -y tesseract-ocr-chi-sim

# 安装英文语言包（通常已包含）
sudo apt-get install -y tesseract-ocr-eng

# 验证安装
tesseract --version
```

#### macOS

```bash
# 使用 Homebrew 安装
brew install tesseract

# 安装语言包
brew install tesseract-lang

# 验证安装
tesseract --version
```

---

### 步骤 2: 添加 Maven 依赖

在项目的 `pom.xml` 中添加 Tess4J 依赖:

```xml
<!-- Tesseract OCR 支持 -->
<dependency>
    <groupId>net.sourceforge.tess4j</groupId>
    <artifactId>tess4j</artifactId>
    <version>5.9.0</version>
</dependency>
```

**完整示例**:

```xml
<dependencies>
    <!-- 现有依赖 -->
    <!-- ... -->

    <!-- 添加 Tesseract OCR 支持 -->
    <dependency>
        <groupId>net.sourceforge.tess4j</groupId>
        <artifactId>tess4j</artifactId>
        <version>5.9.0</version>
    </dependency>
</dependencies>
```

**刷新依赖**:

```bash
# Maven
mvn clean install

# 或在 IDE 中刷新
```

---

### 步骤 3: 配置应用

#### 方式A: 修改 application.yml

编辑 `src/main/resources/application.yml`:

```yaml
knowledge:
  qa:
    image-processing:
      # 启用 OCR 策略
      strategy: ocr
      enable-ocr: true
      
      ocr:
        # Tesseract 数据路径
        # Windows 示例
        tessdata-path: C:\Program Files\Tesseract-OCR\tessdata
        # Linux 示例
        # tessdata-path: /usr/share/tesseract-ocr/5/tessdata
        # macOS 示例  
        # tessdata-path: /usr/local/share/tessdata
        
        # 识别语言
        language: chi_sim+eng  # 中文+英文
```

#### 方式B: 使用环境变量（推荐）

**设置环境变量**:

```bash
# Windows (PowerShell)
$env:TESSDATA_PREFIX="C:\Program Files\Tesseract-OCR\tessdata"

# Windows (CMD)
set TESSDATA_PREFIX=C:\Program Files\Tesseract-OCR\tessdata

# Linux/macOS
export TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata
```

**application.yml 配置**:

```yaml
knowledge:
  qa:
    image-processing:
      strategy: ocr
      enable-ocr: true
      ocr:
        tessdata-path: ${TESSDATA_PREFIX:}  # 从环境变量读取
        language: chi_sim+eng
```

---

## ▶️ 运行应用

### 1. 启动应用

```bash
cd D:\Jetbrains\hackathon\AI-Reviewer\ai-reviewer-base-file-rag

# 使用 Maven 启动
mvn spring-boot:run
```

### 2. 查看启动日志

**成功启动日志**:

```
🖼️  初始化图片处理功能...
   配置策略: ocr
   添加 OCR 策略:
      - Tessdata路径: C:\Program Files\Tesseract-OCR\tessdata
      - 识别语言: chi_sim+eng
   ✅ OCR 策略可用
✅ 图片处理策略已激活: Tesseract OCR
```

**如果出现问题**:

```
⚠️  OCR 策略不可用: 缺少 tess4j 依赖
💡 提示: 添加依赖 net.sourceforge.tess4j:tess4j:5.9.0
```
→ 检查 Maven 依赖是否正确添加

```
⚠️  Tesseract 初始化失败
```
→ 检查 tessdata-path 路径是否正确

---

## 🧪 测试 OCR 功能

### 方式1: 使用命令行测试

```bash
# 测试 Tesseract 安装
tesseract --list-langs

# 应该显示:
# List of available languages (2):
# chi_sim
# eng

# 测试 OCR 识别
tesseract test-image.png output -l chi_sim+eng
cat output.txt
```

### 方式2: 在应用中测试

创建测试文件 `TestOCR.java`:

```java
package top.yumbo.ai.rag.test;

import top.yumbo.ai.rag.impl.parser.image.SmartImageExtractor;
import top.yumbo.ai.rag.impl.parser.image.TesseractOCRStrategy;

import java.io.File;

public class TestOCR {
    public static void main(String[] args) {
        // 创建 OCR 策略
        TesseractOCRStrategy ocr = new TesseractOCRStrategy(
            "C:\\Program Files\\Tesseract-OCR\\tessdata",
            "chi_sim+eng"
        );
        
        // 检查是否可用
        if (ocr.isAvailable()) {
            System.out.println("✅ OCR 可用");
            
            // 测试识别
            File testImage = new File("test-image.png");
            if (testImage.exists()) {
                String text = ocr.extractContent(testImage);
                System.out.println("识别结果: " + text);
            }
        } else {
            System.out.println("❌ OCR 不可用");
        }
    }
}
```

### 方式3: 使用 REST API 测试

```bash
# 1. 上传包含图片的文档
curl -X POST http://localhost:8080/api/files/index \
  -F "file=@document-with-images.pdf"

# 2. 搜索图片中的文字
curl "http://localhost:8080/api/qa/search?query=图片中的文字"
```

---

## 🔧 高级配置

### 1. 语言配置

#### 只识别中文

```yaml
knowledge:
  qa:
    image-processing:
      ocr:
        language: chi_sim  # 只识别简体中文
```

#### 只识别英文

```yaml
knowledge:
  qa:
    image-processing:
      ocr:
        language: eng  # 只识别英文
```

#### 多语言混合

```yaml
knowledge:
  qa:
    image-processing:
      ocr:
        language: chi_sim+chi_tra+eng  # 简体+繁体+英文
```

### 2. 自定义 Tesseract 路径

```yaml
knowledge:
  qa:
    image-processing:
      ocr:
        # 指定完整路径
        tessdata-path: D:\OCR\tessdata
        language: chi_sim+eng
```

### 3. 使用代码配置

```java
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import top.yumbo.ai.rag.impl.parser.image.*;

@Configuration
public class CustomOCRConfig {
    
    @Bean
    public SmartImageExtractor customImageExtractor() {
        SmartImageExtractor extractor = new SmartImageExtractor();
        
        // 添加自定义 OCR 策略
        TesseractOCRStrategy ocr = new TesseractOCRStrategy(
            "C:\\Program Files\\Tesseract-OCR\\tessdata",
            "chi_sim+eng"
        );
        extractor.addStrategy(ocr);
        
        return extractor;
    }
}
```

---

## 📥 下载语言包

### 自动下载（推荐）

在安装 Tesseract 时勾选语言包即可。

### 手动下载

#### 1. 下载语言包文件

访问: https://github.com/tesseract-ocr/tessdata

常用语言包:
- `chi_sim.traineddata` - 简体中文
- `chi_tra.traineddata` - 繁体中文
- `eng.traineddata` - 英文
- `jpn.traineddata` - 日文
- `kor.traineddata` - 韩文

#### 2. 下载命令

```bash
# 简体中文
wget https://github.com/tesseract-ocr/tessdata/raw/main/chi_sim.traineddata

# 英文
wget https://github.com/tesseract-ocr/tessdata/raw/main/eng.traineddata
```

#### 3. 放置语言包

将下载的 `.traineddata` 文件放到 tessdata 目录:

**Windows**:
```
C:\Program Files\Tesseract-OCR\tessdata\
```

**Linux**:
```
/usr/share/tesseract-ocr/5/tessdata/
```

**macOS**:
```
/usr/local/share/tessdata/
```

#### 4. 验证语言包

```bash
tesseract --list-langs
```

---

## ⚠️ 常见问题

### 问题1: OCR 策略不可用

**错误信息**:
```
⚠️  OCR 策略不可用: 缺少 tess4j 依赖
```

**解决方案**:
1. 检查 `pom.xml` 是否添加了依赖
2. 运行 `mvn clean install`
3. 刷新 IDE 项目

### 问题2: 找不到 tessdata

**错误信息**:
```
Error: tessdata directory not found
```

**解决方案**:
```bash
# 1. 查找 tessdata 位置
# Windows
dir "C:\Program Files\Tesseract-OCR\tessdata"

# Linux
ls /usr/share/tesseract-ocr/*/tessdata

# 2. 设置正确的路径
export TESSDATA_PREFIX=/正确的/路径/tessdata
```

### 问题3: 语言包不存在

**错误信息**:
```
Error: Language 'chi_sim' not found
```

**解决方案**:
1. 检查语言包是否安装:
   ```bash
   tesseract --list-langs
   ```

2. 如果没有，手动下载并放置:
   ```bash
   wget https://github.com/tesseract-ocr/tessdata/raw/main/chi_sim.traineddata
   # 移动到 tessdata 目录
   ```

### 问题4: 识别准确度低

**优化方案**:

1. **提高图片质量**:
   - 确保图片清晰
   - 分辨率至少 300 DPI
   - 避免倾斜和模糊

2. **调整语言设置**:
   ```yaml
   # 只识别需要的语言
   language: chi_sim  # 而不是 chi_sim+eng
   ```

3. **使用更好的训练数据**:
   - `tessdata_best` - 最高准确度（慢）
   - `tessdata` - 标准版（推荐）
   - `tessdata_fast` - 快速版（准确度低）

### 问题5: 内存占用高

**解决方案**:

```yaml
knowledge:
  qa:
    document:
      # 限制最大文件大小
      max-file-size-mb: 50
      # 限制最大内容大小
      max-content-size-mb: 20
```

---

## 📊 性能优化

### 1. 批量处理优化

```java
// 批量处理时复用 OCR 实例
SmartImageExtractor extractor = SmartImageExtractor.withOCR();

for (File file : files) {
    String text = extractor.extractContent(file);
    // 处理文本
}
```

### 2. 并行处理

```java
List<File> images = getImages();

// 使用并行流
images.parallelStream()
    .map(file -> extractor.extractContent(file))
    .forEach(text -> processText(text));
```

### 3. 缓存结果

```java
// 使用缓存避免重复识别
Map<String, String> cache = new ConcurrentHashMap<>();

String getText(File file) {
    String key = file.getAbsolutePath();
    return cache.computeIfAbsent(key, 
        k -> extractor.extractContent(file));
}
```

---

## 📈 性能指标

| 场景 | 处理时间 | 准确度 |
|------|---------|--------|
| 清晰打印文字 | 100-300ms | 95%+ |
| 手写文字 | 200-500ms | 70%+ |
| 低分辨率图片 | 300-800ms | 60%+ |
| 倾斜/模糊 | 500ms-1s | 40%+ |

---

## 🎯 实战示例

### 示例1: 识别身份证

```java
TesseractOCRStrategy ocr = new TesseractOCRStrategy(
    tessdataPath, 
    "chi_sim+eng"  // 中文+数字+英文
);

File idCard = new File("id-card.jpg");
String text = ocr.extractContent(idCard);

// 解析结果
// 姓名: XXX
// 身份证号: 123456789012345678
```

### 示例2: 识别发票

```java
TesseractOCRStrategy ocr = new TesseractOCRStrategy(
    tessdataPath,
    "chi_sim"  // 主要是中文
);

File invoice = new File("invoice.png");
String text = ocr.extractContent(invoice);

// 提取发票信息
Pattern pattern = Pattern.compile("发票号码：(\\d+)");
Matcher matcher = pattern.matcher(text);
if (matcher.find()) {
    String invoiceNo = matcher.group(1);
    System.out.println("发票号: " + invoiceNo);
}
```

### 示例3: 批量处理表格

```java
SmartImageExtractor extractor = SmartImageExtractor.withOCR(
    tessdataPath,
    "chi_sim+eng"
);

File excelWithImages = new File("data.xlsx");

// TikaDocumentParser 会自动使用 OCR 处理图片
TikaDocumentParser parser = new TikaDocumentParser();
String content = parser.parse(excelWithImages);

// content 中包含了 OCR 识别的图片文字
```

---

## 🔒 生产环境建议

### 1. 容器化部署

**Dockerfile**:

```dockerfile
FROM openjdk:17-jdk-slim

# 安装 Tesseract
RUN apt-get update && \
    apt-get install -y tesseract-ocr \
                       tesseract-ocr-chi-sim \
                       tesseract-ocr-eng && \
    rm -rf /var/lib/apt/lists/*

# 设置环境变量
ENV TESSDATA_PREFIX=/usr/share/tesseract-ocr/5/tessdata

# 复制应用
COPY target/app.jar /app.jar

# 启动
CMD ["java", "-jar", "/app.jar"]
```

### 2. 监控和告警

```yaml
# 监控 OCR 性能
knowledge:
  qa:
    image-processing:
      ocr:
        # 添加超时设置
        timeout-seconds: 30
```

### 3. 资源限制

```yaml
# JVM 参数
JAVA_OPTS: "-Xmx2g -Xms1g"

# 并发限制
knowledge:
  qa:
    document:
      max-concurrent-tasks: 4
```

---

## ✅ 检查清单

使用此清单确保 OCR 正确配置:

- [ ] ✅ Tesseract 已安装
- [ ] ✅ 语言包已下载
- [ ] ✅ TESSDATA_PREFIX 已设置
- [ ] ✅ Maven 依赖已添加
- [ ] ✅ application.yml 已配置
- [ ] ✅ 能看到 "OCR 策略可用" 日志
- [ ] ✅ 测试识别成功

---

## 📚 相关资源

### 官方文档
- Tesseract GitHub: https://github.com/tesseract-ocr/tesseract
- Tesseract Wiki: https://github.com/tesseract-ocr/tesseract/wiki
- Tess4J: https://github.com/nguyenq/tess4j

### 语言包下载
- 标准版: https://github.com/tesseract-ocr/tessdata
- 最佳版: https://github.com/tesseract-ocr/tessdata_best
- 快速版: https://github.com/tesseract-ocr/tessdata_fast

### 问题排查
- Tesseract Issues: https://github.com/tesseract-ocr/tesseract/issues
- Tess4J Issues: https://github.com/nguyenq/tess4j/issues

---

## 🎉 总结

通过以上步骤，你已经可以:

1. ✅ 安装和配置 Tesseract OCR
2. ✅ 在 LocalFileRAG 中启用 OCR
3. ✅ 识别图片中的文字
4. ✅ 优化 OCR 性能
5. ✅ 解决常见问题

**下一步**:
- 尝试处理包含图片的文档
- 根据需要调整语言和参数
- 考虑升级到混合模式（OCR + Vision LLM）

**需要帮助?**
- 查看文档: `20251123003000_图片转文本功能实现方案.md`
- 查看集成文档: `20251123004000_图片处理功能集成完成.md`

---

**文档版本**: v1.0  
**更新时间**: 2025-11-23  
**适用版本**: LocalFileRAG v1.0+

