# 📝 下载脚本开关功能说明

## ✅ 功能已实现

已添加配置开关来控制是否使用下载脚本进行文件同步。

## 🔧 配置说明

### application.yml 配置

```yaml
ai-reviewer:
  batch:
    # 是否启用下载脚本（true=启用脚本下载，false=仅Web上传模式）
    enable-download-script: false    # 默认为 false
    download-script-path: /home/jinhua/AI-Reviewer/download
    scan-interval-minutes: 1
```

### 配置选项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `enable-download-script` | 是否启用下载脚本 | `false` |
| `download-script-path` | 下载脚本的路径 | `/home/jinhua/AI-Reviewer/download` |
| `scan-interval-minutes` | 扫描间隔（分钟） | `1` |

## 📋 两种工作模式

### 模式1: 仅Web上传模式（默认）

```yaml
enable-download-script: false
```

**特点：**
- ✅ 团队只能通过Web界面上传文件
- ✅ 不会执行下载脚本
- ✅ 系统每1分钟扫描一次done.txt文件
- ✅ 适合纯Web上传场景

**日志输出：**
```
Starting continuous batch review for all projects in: /path/to/projects
Download script mode: DISABLED (Web upload only)
Will rescan every 1 minutes
Download script disabled, skipping...
```

### 模式2: 下载脚本模式

```yaml
enable-download-script: true
```

**特点：**
- ✅ 每次扫描前先执行下载脚本
- ✅ 从远程同步文件到本地
- ✅ 支持Web上传和脚本下载混合使用
- ✅ 适合需要远程同步的场景

**日志输出：**
```
Starting continuous batch review for all projects in: /path/to/projects
Download script mode: ENABLED
Will rescan every 1 minutes
Executing download script...
Executing download script: /home/jinhua/AI-Reviewer/download
```

## 🚀 使用场景

### 场景1: 只用Web上传（推荐新项目）

```yaml
# application.yml
ai-reviewer:
  batch:
    enable-download-script: false
    scan-interval-minutes: 1
```

**适用于：**
- 新搭建的Hackathon评审系统
- 所有团队通过Web界面提交
- 不需要远程文件同步

### 场景2: 混合模式

```yaml
# application.yml
ai-reviewer:
  batch:
    enable-download-script: true
    download-script-path: /home/jinhua/AI-Reviewer/download
    scan-interval-minutes: 2
```

**适用于：**
- 需要从远程服务器同步文件
- 同时支持Web上传和脚本下载
- 有现成的下载脚本需要保留

## 🔄 工作流程

### Web上传模式流程（enable-download-script: false）

```
启动应用
    ↓
每1分钟循环：
    ↓
跳过下载脚本
    ↓
扫描done.txt文件
    ↓
发现新提交 → 解压ZIP → AI评分 → 生成报告
    ↓
等待1分钟
    ↓
继续循环...
```

### 下载脚本模式流程（enable-download-script: true）

```
启动应用
    ↓
每1分钟循环：
    ↓
执行下载脚本（同步文件）
    ↓
等待2秒（文件系统同步）
    ↓
扫描done.txt文件
    ↓
发现新提交 → 解压ZIP → AI评分 → 生成报告
    ↓
等待1分钟
    ↓
继续循环...
```

## 📝 配置修改步骤

### 1. 编辑配置文件

```bash
# 编辑 application.yml
vim src/main/resources/application.yml
```

### 2. 修改配置

```yaml
ai-reviewer:
  batch:
    enable-download-script: false  # 改为 true 或 false
```

### 3. 重新打包

```bash
mvn clean package -DskipTests
```

### 4. 重启应用

```bash
# 停止当前应用
Ctrl+C

# 启动新版本
java -jar target/hackathonApplication.jar --reviewAll=/path/to/projects
```

## 🔍 验证配置

启动应用后，查看日志确认模式：

**Web上传模式：**
```
Download script mode: DISABLED (Web upload only)
Download script disabled, skipping...
```

**下载脚本模式：**
```
Download script mode: ENABLED
Executing download script...
```

## ⚠️ 注意事项

### 1. 脚本文件检查

如果启用下载脚本模式（`enable-download-script: true`），确保：
- ✅ 脚本文件存在：`/home/jinhua/AI-Reviewer/download`
- ✅ 脚本有执行权限：`chmod +x /home/jinhua/AI-Reviewer/download`
- ✅ 脚本路径正确配置

### 2. 扫描间隔

根据实际需求调整扫描间隔：
- **Web上传模式：** 建议1分钟（响应快）
- **下载脚本模式：** 建议2-5分钟（减少脚本执行频率）

### 3. 性能考虑

- Web上传模式更高效（无需执行脚本）
- 下载脚本模式适合需要远程同步的场景
- 扫描间隔越短，系统负载越高

## 📊 对比总结

| 特性 | Web上传模式 | 下载脚本模式 |
|------|------------|-------------|
| 文件来源 | Web界面上传 | 脚本下载 + Web上传 |
| 执行脚本 | ❌ 不执行 | ✅ 每次扫描前执行 |
| 响应速度 | ⚡ 快（1分钟） | 🐢 较慢（需等待脚本） |
| 系统负载 | ✅ 低 | ⚠️ 中等 |
| 推荐场景 | 新项目、纯Web | 需要远程同步 |
| 配置 | `false` | `true` |

## 🎯 推荐配置

### 生产环境（纯Web上传）

```yaml
ai-reviewer:
  batch:
    enable-download-script: false
    scan-interval-minutes: 1
```

### 生产环境（混合模式）

```yaml
ai-reviewer:
  batch:
    enable-download-script: true
    download-script-path: /home/jinhua/AI-Reviewer/download
    scan-interval-minutes: 2
```

---

✅ **功能已完成！** 根据需要在 `application.yml` 中修改 `enable-download-script` 配置即可。

