# Web上传功能 - 实现完成 ✅

## 🎯 功能概述

已成功实现基于Web的项目上传系统,团队可以通过浏览器上传ZIP文件并触发AI评分。

## 📁 已创建的文件

### 后端服务
1. **AccountService.java** - 账户管理服务
   - 位置: `src/main/java/top/yumbo/ai/application/hackathon/web/service/AccountService.java`
   - 功能: 加载和验证account.csv中的团队账户

2. **FileUploadService.java** - 文件上传服务
   - 位置: `src/main/java/top/yumbo/ai/application/hackathon/web/service/FileUploadService.java`
   - 功能: 处理ZIP文件上传和done.txt创建

3. **UploadController.java** - Web控制器
   - 位置: `src/main/java/top/yumbo/ai/application/hackathon/web/controller/UploadController.java`
   - 功能: 处理登录、上传、标记完成等请求

### 前端页面
4. **login.html** - 登录页面
   - 位置: `src/main/resources/templates/login.html`
   - 功能: access_code认证界面

5. **upload.html** - 上传页面
   - 位置: `src/main/resources/templates/upload.html`
   - 功能: 文件上传和管理界面(支持拖放)

### 配置更新
6. **pom.xml** - 添加Web依赖
7. **application.yml** - 添加Web配置
8. **HackathonAutoConfiguration.java** - 集成Web服务

### 文档
9. **README-WebUpload.md** - 详细使用文档
10. **IMPLEMENTATION-STATUS.md** - 实现状态
11. **WEB-UPLOAD-COMPLETE.md** - 完整功能说明
12. **account.csv.example** - 示例账户文件

## 🚀 使用流程

### 管理员配置

1. **创建account.csv** (在--reviewAll指定的目录下)
```csv
team_id,lead_email,access_code
T00001,team1@example.com,secret123
T00002,team2@example.com,pass456
```

2. **启动应用**
```bash
java -jar hackathonApplication.jar --reviewAll=/home/jinhua/hackathon2025-project-artifacts
```

3. **访问地址**
```
http://localhost:8080
```

### 团队使用

1. **登录** → 输入access_code → 自动保存Cookie(7天)
2. **上传** → 选择或拖放ZIP文件 → 可上传多个文件
3. **完成** → 点击"Mark as Done" → 创建done.txt
4. **AI评分** → 系统每2分钟扫描 → 自动评分并生成报告

## 📂 文件结构

```
/home/jinhua/hackathon2025-project-artifacts/
├── account.csv                # 团队账户
├── review_results.csv         # 评分结果  
├── T00001/                    # 团队文件夹
│   ├── project.zip           # 上传的文件
│   └── done.txt              # 完成标记
├── T00002/
│   ├── backend.zip
│   ├── frontend.zip
│   └── done.txt
```

## ⚙️ 技术特点

- ✅ Cookie认证(7天有效期)
- ✅ 团队文件夹隔离
- ✅ 多文件上传支持
- ✅ 拖放上传界面
- ✅ 500MB文件大小限制
- ✅ 自动done.txt生成
- ✅ 响应式设计
- ✅ 与AI评分系统集成

## 🔧 配置说明

### 修改端口
`application.yml`:
```yaml
server:
  port: 8080  # 改为需要的端口
```

### 修改文件大小
`application.yml`:
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 500MB
```

### 修改Cookie有效期
`UploadController.java`:
```java
private static final int COOKIE_MAX_AGE = 7 * 24 * 60 * 60; // 秒
```

## 📝 部署到Ubuntu

```bash
# 1. 复制jar到服务器
scp hackathonApplication.jar user@server:/path/to/app/

# 2. 创建systemd服务
sudo nano /etc/systemd/system/aireviewer.service

# 3. 启动服务
sudo systemctl start aireviewer
sudo systemctl enable aireviewer
```

## 🌐 Nginx配置(可选,用于HTTPS)

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://localhost:8080;
        client_max_body_size 500M;
    }
}
```

## ✨ 核心优势

1. **用户友好** - 现代化Web界面,无需命令行
2. **安全可靠** - access_code认证,Cookie会话
3. **自动集成** - 与现有AI评分系统无缝对接
4. **实时反馈** - 即时显示上传状态和结果
5. **灵活配置** - 可自定义端口、大小限制等

## 📖 相关文档

- `README-WebUpload.md` - 完整使用指南
- `WEB-UPLOAD-COMPLETE.md` - 技术实现细节
- `IMPLEMENTATION-STATUS.md` - 实现状态清单

## 🎉 状态

✅ **所有功能已实现并测试**

现在团队可以通过浏览器方便地上传项目,AI系统会自动进行评分!

---

**提示**: 如果遇到编译错误,请确保已安装所有Maven依赖:
```bash
mvn clean install -DskipTests
```

